# YOLOv5 🚀 by Ultralytics, GPL-3.0 license

# Parameters
nc: 80  # number of classes
depth_multiple: 0.33  # model depth multiple
width_multiple: 0.50  # layer channel multiple
anchors:
  - [10,13, 16,30, 33,23]  # P3/8
  - [30,61, 62,45, 59,119]  # P4/16
  - [116,90, 156,198, 373,326]  # P5/32

# YOLOv5 v6.0 backbone
backbone:
  # [from, number, module, args]
  [[-1, 1, Conv, [64, 6, 2, 2]],  # 0-P1/2
   [-1, 1, GhostConv, [128, 3, 2]],  # 1-P2/4
   [-1, 3, CoordAtt, [128]] ,  #tag:增加CoordAtt
   [-1, 3, C3Ghost, [128]],
   [-1, 1, GhostConv, [256, 3, 2]],  # 3-P3/8
   [-1, 3, CoordAtt, [256]] ,  #tag:增加CoordAtt
   [-1, 6, C3Ghost, [256]],
   [-1, 1, GhostConv, [512, 3, 2]],  # 5-P4/16
   [-1, 3, CoordAtt, [512]] ,  #tag:增加CoordAtt
   [-1, 9, C3Ghost, [512]],
   [-1, 1, GhostConv, [1024, 3, 2]],  # 7-P5/32
   [-1, 3, CoordAtt, [1024]] ,  #tag:增加CoordAtt
   [-1, 3, C3Ghost, [1024]],
   [-1, 1, SPPF, [1024, 5]],  # 13层
  ]

# YOLOv5 v6.0 head
head:
  [[-1, 1, GhostConv, [512, 1, 1]],       #tag:输出256个通道
   [-1, 1, nn.Upsample, [None, 2, 'nearest']],
   [[-1, 9], 1, BiFPN_Add2, [256, 256]],  # cat backbone P4   #tag：concat变为512个通道,1/16的两个特征图相加
   [-1, 3, C3Ghost, [512, False]],  # 13      #tag:降维度输出256个通道

   [-1, 1, GhostConv, [256, 1, 1]],
   [-1, 1, nn.Upsample, [None, 2, 'nearest']],
   [[-1, 6], 1, BiFPN_Add2, [128,128]],  # cat backbone P3   两个1/8的特征图相加
   [-1, 3, C3Ghost, [256, False]],  # 17 (P3/8-small)
   [-1,1,SE,[256]],

   [-1, 1, GhostConv, [512, 3, 2]],
   [[-1, 17,9], 1, BiFPN_Add3, [256,256]],  # cat head P4    三个1/16的特征图相加
   [-1, 3, C3Ghost, [512, False]],  # 20 (P4/16-medium)
   [-1,1,SE,[512]],

   [-1, 1, GhostConv, [512, 3, 2]],
   [[-1, 14], 1, BiFPN_Add2, [256,256]],  # cat head P5
   [-1, 3, C3Ghost, [1024, False]],  # 23 (P5/32-large)
   [-1,1,SE,[1024]],

   [[22, 26, 30], 1, Detect, [nc, anchors]],  # Detect(P3, P4, P5)
  ]
